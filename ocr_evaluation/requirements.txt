# ========================================
# OCR Evaluation — Dependencies
# ========================================
# Install: pip install -r ocr_evaluation/requirements.txt

# ========================================
# Core (always needed)
# ========================================
loguru>=0.7.0
pdfplumber>=0.11.0
jiwer>=3.0.0
pdf2image>=1.17.0
Pillow>=10.0.0
numpy>=1.26.0
tqdm>=4.66.0

# ========================================
# Tesseract OCR
# ========================================
# Also requires system package: sudo apt-get install tesseract-ocr tesseract-ocr-por
pytesseract>=0.3.10
opencv-python-headless>=4.9.0

# ========================================
# EasyOCR
# ========================================
easyocr>=1.7.0

# ========================================
# docTR (Mindee)
# ========================================
python-doctr[torch]>=0.9.0

# ========================================
# unstructured
# ========================================
unstructured[pdf]>=0.15.0

# ========================================
# Chandra OCR 2
# ========================================
# chandra-ocr  # Uncomment if available; package may not be on PyPI

# ========================================
# PaddleOCR
# ========================================
# Avoid Paddle 3.3.x + PaddleOCR 3.4.x runtime incompatibility on CPU
# (ConvertPirAttribute2RuntimeAttribute in onednn_instruction.cc:116).
paddlepaddle>=3.2.1,<3.3.0
paddleocr>=3.3.0,<3.4.0
